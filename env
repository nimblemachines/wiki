#!/usr/bin/perl

# $Id$

# print out the CGI environment

print "Content-type: text/plain\n\n";
foreach $key (sort keys %ENV) {
  print "$key = $ENV{$key}\n";
}
print "PWD = " . `pwd`;
print "ID = " . `id`;

